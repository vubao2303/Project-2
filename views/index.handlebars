<!-- Main Nav -->
<nav class="navbar navbar-expand-lg py-3 px-4 auto-hiding-navbar">
  <h4 class="mr-auto">Party-X</h4>
  <a class="ml-auto px-3" href="#">Logout</a>
</nav>
<!-- End Main Nav -->

{{!-- --}}
{{!-- --}}
{{!-- --}}

<!-- Body Content -->
<div class="holder">
  <p class="user">{{User.name}}</p>
  <div class="row">

    <!-- Create Events Card -->
    <div class="col-md-6 col-style">
      <div class="card" id="createEvent">
        <div class="card-body">
          <h5 class="card-title">My Event</h5>
          <hr />
          <ul class="userEvents">
            {{!-- List items --}}
            {{!-- {{#each}}
            {{#unless}}
            {{>}}
            {{/unless}}
            {{/each}} --}}
          </ul>
          <!-- Button trigger modal -->
          <button type="button" class="create-btn" data-toggle="modal" data-target="#myModal">
            Create Event
          </button>
        </div>
      </div>
    </div>
    <!-- End Of Create Events Card -->

    {{!-- --}}
    {{!-- --}}
    {{!-- --}}

    <!-- Upcoming Events Card -->
    <div class="col-sm-6 col-style">
      <div class="card" id="upcoming">
        <div class="card-body">
          <h5 class="card-title">Upcoming Events</h5>
          <hr />
          <ul class="attendEvents">
            {{!-- List items --}}
            {{!-- {{#unless}}
            {{#each}}
            {{>}}
            {{/unless}}
            {{/each}} --}}
          </ul>
        </div>
      </div>
    </div>
    <!-- End of Upcoming Events Card -->
  </div>

  {{!-- --}}
  {{!-- --}}
  {{!-- --}}

  <!-- All events Content -->
  <div class="row">
    <div class="col col-md-1"></div>
    <!-- IGNORE TOP COL -->
    <div class="col col-md-10">
      <div class="card" id="allEvents">
        <div class="card-body">
          <h5 class="card-title">All Events</h5>
          <hr />
          <ul class="allEvents-list">
            <!-- <li>Populated Events</li> -->
            {{!-- {{#unless}}
            {{#each}}
            {{>}}
            {{/unless}}
            {{/each}} --}}
          </ul>
        </div>
      </div>
    </div>
    <div class="col col-md-1"></div>
    <!-- IGNORE TOP COL-->
  </div>
  <!-- End All Events -->
</div>
<!-- End Body Content -->

{{!-- --}}
{{!-- --}}
{{!-- --}}

<!-- Ceate Event Modal -->

<!-- Modal -->
<div class="modal fade" id="myModal" data-backdrop="static" data-keyboard="false" tabindex="-1"
  aria-labelledby="staticBackdropLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="staticBackdropLabel">The party starts here!</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
          <form class="create-form">
            <div class="form-row">
              <div class="form-group col-md-12">
                <label for="eventTitle">Event</label>
                <input type="text" class="form-control" id="eventTitle" placeholder="" />
              </div>
              <div class="form-group col-md-12">
                <label for="eventTheme">Theme</label>
                <input type="text" class="form-control" id="eventTheme" placeholder="" />
              </div>
            </div>
            <div class="form-group">
              <label for="eventLoc">Location</label>
              <input type="text" class="form-control" id="eventLoc" placeholder="Event location" />
            </div>
            <div class="form-group col-md-6">
                <label for="eventDate">Date</label>
                <input type="date" class="form-control" id="eventDate" />
              </div>
              <div class="form-group col-md-6">
                <label for="eventTime">Time</label>
                <input type="time" class="form-control" id="eventTime" />
              </div>
          </form>
      </div>
      <div class="modal-footer">


        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="submit" id="createPartyBtn" class="btn btn-primary">Submit</button>

      </div>
    </div>
  </div>
</div>
<!-- End Event Modal -->